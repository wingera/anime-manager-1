# 密钥加密方案

需要保护的内容：TMDB 访问钥匙、qBittorrent 地址、账号、密码、通知服务密钥、代理认证信息。

系统启动时从 `APP_SECRET_KEY` 派生加密密钥。网页保存敏感设置前，后端先加密再写入数据库。

日志、接口返回、前端状态不得显示完整密钥。展示时只允许显示前 3 位和后 3 位，中间使用星号。

更换 `APP_SECRET_KEY` 前必须先导出加密设置，再使用新密钥重新导入；否则旧密钥加密的数据无法解密。
